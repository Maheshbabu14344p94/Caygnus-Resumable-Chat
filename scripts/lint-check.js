import fs from 'node:fs';import path from 'node:path';function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>{const p=path.join(d,e.name);return e.isDirectory()?walk(p):[p]})}const files=walk('server').concat(walk('client')).filter(f=>/\.(js|html|css)$/.test(f));let bad=false;for(const f of files){const s=fs.readFileSync(f,'utf8');if(/TODO\s*:\s*FIXME/.test(s)){console.error(`marker: ${f}`);bad=true}}console.log(`Checked ${files.length} source files.`);process.exit(bad?1:0)
